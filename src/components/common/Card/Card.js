const Card = ({children}) => {
    return (
        <div className="bg-gray-100 border-0 border-white rounded-2xl mx-auto h-64 shadow-xl shadow">
            {children}
        </div>
    )
}

export default Card
